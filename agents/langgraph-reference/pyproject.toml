[project]
name = "trazo-langgraph-reference"
version = "0.0.1"
description = "LangGraph reference agent that emits trazo-format traces"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
